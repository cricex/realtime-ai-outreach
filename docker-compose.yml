services:
  app:
    build: .
    ports:
      - "${WEBSITES_PORT:-8000}:8000"
    env_file:
      - .env
      - .env.local
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
